// Buffa-generated proto message types, produced by `buf generate --template
// buf.gen.files.yaml` from the vendored `proto/portal/portal/files/v1/svc.proto`
// (the portal `FilesService` contract). Types only — no ConnectRPC service — the
// wasm files engine drives object_store directly and only needs the message
// shapes. Regenerate with `just generate-files-proto`; never hand-edit the
// `buffa/*.rs` files.
//
// The module tree mirrors the proto package path (`portal.files.v1`) so callers
// reference `generated::portal::files::v1::FileMetadata`.

#[allow(
    non_camel_case_types,
    dead_code,
    unused_imports,
    unused_qualifications,
    clippy::derivable_impls,
    clippy::match_single_binding,
    clippy::uninlined_format_args,
    clippy::doc_lazy_continuation,
    clippy::module_inception
)]
pub mod buffa {
    pub mod portal {
        pub mod files {
            pub mod v1 {
                include!("buffa/portal.files.v1.rs");
            }
        }
    }
}

pub use buffa::portal;

/// ConnectRPC service trait + dispatcher for `portal.files.v1.FilesService`,
/// generated by `protoc-gen-connect-rust`. Referenced by the in-wasm dispatch
/// (see `crate::files::service`). Points back at the buffa message types above.
#[allow(
    dead_code,
    unused_imports,
    unused_qualifications,
    clippy::all,
    clippy::pedantic
)]
pub mod connect {
    pub mod portal {
        pub mod files {
            pub mod v1 {
                include!("connect/portal.files.v1.mod.rs");
            }
        }
    }
}
